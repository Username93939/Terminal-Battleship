# Terminal Battleship
This is a terminal version of the board game, complete with a fairly competent bot

To run it, either download the pre-compiled executables or compile it with 

```g++ *.cpp -o "Battleship"```

then run

```./Battleship```